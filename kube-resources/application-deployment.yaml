apiVersion: apps/v1
kind: Deployment
metadata:
  name: application
spec:
  replicas: 3
  selector:
    matchLabels:
      app: application
  template:
    metadata:
      labels:
        app: application
    spec:
      containers:
        - name: application
          image: web_app_test_image_minikube
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 8080